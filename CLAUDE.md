1. 请阅读我的main6-n-step_reinforcement.py代码以及L2S目录下所有文件，了解我整个训练过程；
2. 请你针对我的代码进行修改，注意不直接在原代码上直接进行修改，你先复制一份代码（可以自己新建一个目录，假设是L2S_PPO），然后在这个复制的文件中修改代码，修改要求如下：
3. 将我原有的训练代码由reinforce修改为PPO，并将reward的设计返回为原本的reward之间的差值
4. 根据3的修改，将我训练的pipeline同步修改（可以新建一个训练文件main7-n-step_reinforcement_ppo.py），保证接口正确并跑通。
5. 暂时不需要你直接进行训练，以及注意所有的修改都是复制后修改，不要改动我原有的代码
